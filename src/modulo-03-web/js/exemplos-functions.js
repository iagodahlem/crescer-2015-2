/* ======================================= *
 *	Exemplos de funções JavaScript         *
 * ======================================= */

// Função auto declaravel
(function(texto){
	alert(texto);
})('oi');

throw new Error('');

var arr = ['a', 'b', 'c'];
for (var prop in arr) {
	console.log(arr[prop]);
};

// FOR EACH
exemplo.forEach() {
	function(elem, i) {
		console.log();
	}
}