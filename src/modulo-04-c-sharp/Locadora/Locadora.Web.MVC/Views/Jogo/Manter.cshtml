@model JogoModel

@{ string titulo = Model != null ? "Editar Jogo" : "Cadastrar Jogo"; }

@{
    ViewBag.Title = titulo;
}

<h2 class="text-center">@titulo</h2>

@using (@Html.BeginForm("Salvar", "Jogo", FormMethod.Post, new { @class = "form-horizontal" }))
{

}
